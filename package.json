{
  "name": "bolsapolis",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "RTS de finanzas: tu portafolio es tu imperio. La accion sube -> crece tu ciudad.",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "phaser": "^3.80.1"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "vite": "^5.4.2",
    "vitest": "^2.0.5"
  }
}
